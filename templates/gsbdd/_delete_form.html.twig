<form method="post" action="{{ path('app_g_s_b_d_d_delete', {'id': g_s_b_d_d.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ g_s_b_d_d.id) }}">
    <button class="btn">Delete</button>
</form>
