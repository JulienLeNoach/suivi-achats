{% extends 'base.html.twig' %}

{% block title %}GSBDD{% endblock %}

{% block body %}
    <h1>GSBDD</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ g_s_b_d_d.id }}</td>
            </tr>
            <tr>
                <th>Libelle_gsbdd</th>
                <td>{{ g_s_b_d_d.libelleGsbdd }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_g_s_b_d_d_index') }}">back to list</a>

    <a href="{{ path('app_g_s_b_d_d_edit', {'id': g_s_b_d_d.id}) }}">edit</a>

    {{ include('gsbdd/_delete_form.html.twig') }}
{% endblock %}
