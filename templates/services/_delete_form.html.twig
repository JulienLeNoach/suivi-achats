<form method="post" action="{{ path('app_services_delete', {'id': service.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir supprimer ce service ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service.id) }}">
    <button class="fr-btn">Supprimer</button>
</form>
