{% extends 'base.html.twig' %}

{% block title %}Utilisateur{% endblock %}

{% block body %}
<div class="achat-container">
    <div class='align-items-center justify-content-center d-flex mt-3 bandeau-form-search'>
        <h5 class="mt-4 ms-3 blueF">Utilisateur {{utilisateur.trigram}}</h5>
        <div class="bande ms-3">
        </div>
    </div>
    <div class="action-container p-3">

    <table class="">
        <tbody>
            <tr>
                <th>Nom Connexion</th>
                <td>{{ utilisateur.nomConnexion }}</td>
            </tr>
            <tr>
                <th>Nom Utilisateur</th>
                <td>{{ utilisateur.nomUtilisateur }}</td>
            </tr>
            <tr>
                <th>Prenom Utilisateur</th>
                <td>{{ utilisateur.prenomUtilisateur }}</td>
            </tr>
            <tr>
                <th>Etat Utilisateur</th>
                <td>{{ utilisateur.etatUtilisateur ? 'Actif' : 'Inactif' }}</td>
            </tr>
            <tr>
                <th>Trigram</th>
                <td>{{ utilisateur.trigram }}</td>
            </tr>
        </tbody>
   </table>

    <div class="align-items-center justify-content-center d-flex mt-3">

    <a class="fr-btn me-3" href="{{ path('app_utilisateurs_edit', {'id': utilisateur.id}) }}">Modifier</a>

    {{ include('utilisateurs/_delete_form.html.twig') }}
    </div>
    <div class="align-items-center justify-content-center d-flex mt-3">
        <a class="fr-btn" href="{{ path('app_utilisateurs_index') }}">Retour à la liste</a>
</div>
    </div>
    </div>

</div>
{% endblock %}